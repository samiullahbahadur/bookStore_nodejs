// import db from "../models/index.js";

// beforeAll(async () => {
//   // Recreate schema from models each test run
//   await db.sequelize.sync({ force: true });
// });

// afterAll(async () => {
//   // Close DB connection when tests are done
//   await db.sequelize.close();
// });



